import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#5e35b1',
      light: '#e8eaf6',
      white: '#ffffff',
    },
    background: {
      default: '#e8eaf6',
      paper: '#ffffff',
    },
  },
});

export default theme;
